* {
  box-sizing: border-box;
}

body {
  font-family: sans-serif;
  margin: 0;
}

#root {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  flex-direction: column;
}

#root > * + * {
  margin-top: 1rem;
}
